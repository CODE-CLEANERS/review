package com.example.spring_games.member.domain.vo;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.when;

class MemberLevelTest {
//    @Test
//    @DisplayName("isGoldLevelEligible() 매서드 리턴값이 true 면 골드 회원으로 간주한다.")
//    void getInstance_GOLD() {
//        // given
//        final Cards mockedCards = Mockito.mock(Cards.class);
//
//        // when
//        when(mockedCards.isGoldLevelEligible())
//                .thenReturn(true);
//
//        assertThat(MemberLevel.calculateMemberLevel(mockedCards)).isEqualTo(MemberLevel.GOLD);
//    }
//
//    @Test
//    @DisplayName("isSilverLevelEligible() 매서드 리턴값이 true 면 실버 회원으로 간주한다.")
//    void getInstance_SILVER() {
//        // given
//        final Cards mockedCards = Mockito.mock(Cards.class);
//
//        // when
//        when(mockedCards.isSilverLevelEligible())
//                .thenReturn(true);
//
//        assertThat(MemberLevel.calculateMemberLevel(mockedCards)).isEqualTo(MemberLevel.SILVER);
//    }
//
//    @Test
//    @DisplayName("isGoldLevelEligible(), isSilverLevelEligible() 매서드 리턴값이 false 면 브론즈 회원으로 간주한다.")
//    void getInstance_BRONZE() {
//        // given
//        final Cards mockedCards = Mockito.mock(Cards.class);
//
//        // when
//        when(mockedCards.isSilverLevelEligible())
//                .thenReturn(false);
//
//        assertThat(MemberLevel.calculateMemberLevel(mockedCards)).isEqualTo(MemberLevel.BRONZE);
//    }
}